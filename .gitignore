# Raw snapshot archives / temp CSVs — regenerated by download_data.py
data/raw/
*.zip
/tmp/

# Large training-only table (regenerated by download_forecast_data.py);
# the app only needs slot_profile.parquet + the trained model.
data/history_15min.parquet

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# OS / editors
.DS_Store
.vscode/
