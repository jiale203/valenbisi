# Pinned for reproducible deploys (Streamlit Community Cloud reads this file).
# scikit-learn must match the version the .pkl models were trained with.
streamlit==1.57.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
plotly==6.6.0
pydeck==0.9.2
joblib==1.5.2
pyarrow==23.0.1
